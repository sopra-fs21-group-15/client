import React from "react";
import styled from "styled-components";
import logo from "../cartoon_pencil.png"

const SkribbleLogo = styled.svg`
  width: ${props => props.width}px;
  height: ${props => props.height}px;
  `;